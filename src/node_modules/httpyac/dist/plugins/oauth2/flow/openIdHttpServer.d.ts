interface RequestListener {
    path: string;
    port: number;
    id: string;
    name: string;
    resolve: (params: Record<string, string>) => {
        valid: boolean;
        message: string;
        statusMessage: string;
    };
    reject: () => void;
}
export declare function registerListener(listener: RequestListener): void;
export declare function unregisterListener(id: string): void;
export {};
