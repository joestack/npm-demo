import * as models from '../../../models';
declare class RefreshTokenFlow {
    private isTokenExpired;
    perform(openIdInformation: models.OpenIdInformation, context: models.OpenIdContext): Promise<models.OpenIdInformation | false>;
}
export declare const refreshTokenFlow: RefreshTokenFlow;
export {};
