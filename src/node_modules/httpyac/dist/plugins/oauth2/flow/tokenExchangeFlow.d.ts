import type * as models from '../../../models';
export declare class TokenExchangeFlow {
    static getCacheKey(config: models.OpenIdConfiguration): string | false;
    static perform(config: models.OpenIdConfiguration, openIdInformation: models.OpenIdInformation, context: models.OpenIdContext): Promise<models.OpenIdInformation | false>;
}
