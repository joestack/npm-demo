import * as models from '../../../models';
export declare function refMetaDataHandler(type: string, name: string | undefined, context: models.ParserContext): boolean;
export interface RefMetaHttpRegionData {
    name: string;
    force: boolean;
}
