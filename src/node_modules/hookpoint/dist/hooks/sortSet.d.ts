export declare class SortSet<T extends {
    id: string;
    before?: Array<string>;
    after?: Array<string>;
}> {
    private items;
    private sortedItems;
    constructor();
    hasItem(id: string): boolean;
    add(...items: Array<T>): void;
    private ensureUniqueId;
    addSortSet(set: SortSet<T>): void;
    remove(id: string): boolean;
    get sorted(): T[];
    private prepareSortItems;
    private calculateIndex;
    private getIndex;
}
