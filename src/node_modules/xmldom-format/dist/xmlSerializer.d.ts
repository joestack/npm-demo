import { FormatOptions } from './models';
export declare class XMLSerializer {
    private readonly formatOptions?;
    constructor(formatOptions?: FormatOptions | undefined);
    serializeToString(node: Node): string;
}
